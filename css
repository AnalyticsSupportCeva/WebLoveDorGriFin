:root {
  --pink: #ff8fab;
  --soft-pink: #ffd6e0;
  --wine: #8f2d56;
  --gold: #f8d49d;
  --white: #ffffff;
  --text: #3d2330;
  --glass: rgba(255, 255, 255, 0.22);
  --glass-border: rgba(255, 255, 255, 0.45);
}

* {
  margin: 0;
  padding: 0;
  box-sizing: border-box;
}

body {
  min-height: 100vh;
  font-family: "Montserrat", sans-serif;
  color: var(--text);
  background:
    radial-gradient(circle at top left, #ffe6ef 0%, transparent 32%),
    radial-gradient(circle at bottom right, #ffc2d1 0%, transparent 35%),
    linear-gradient(135deg, #fff1f5, #fbc4d6, #f7d9e3);
  overflow-x: hidden;
}

.background {
  position: fixed;
  inset: 0;
  z-index: 0;
  overflow: hidden;
}

.light {
  position: absolute;
  border-radius: 50%;
  filter: blur(18px);
  opacity: 0.75;
  animation: floatLight 9s ease-in-out infinite alternate;
}

.light-one {
  width: 260px;
  height: 260px;
  background: rgba(255, 143, 171, 0.55);
  top: 8%;
  left: 8%;
}

.light-two {
  width: 340px;
  height: 340px;
  background: rgba(248, 212, 157, 0.45);
  bottom: 5%;
  right: 7%;
  animation-delay: 1.5s;
}

.light-three {
  width: 180px;
  height: 180px;
  background: rgba(255, 255, 255, 0.55);
  top: 48%;
  right: 20%;
  animation-delay: 3s;
}

@keyframes floatLight {
  from {
    transform: translate3d(0, 0, 0) scale(1);
  }
  to {
    transform: translate3d(35px, -35px, 60px) scale(1.08);
  }
}

.scene {
  position: relative;
  z-index: 2;
  min-height: 100vh;
  padding: 60px 20px;
  display: flex;
  justify-content: center;
  align-items: center;
  perspective: 1600px;
}

.card-3d {
  position: relative;
  width: min(1120px, 100%);
  padding: 52px;
  border-radius: 34px;
  background: linear-gradient(
    145deg,
    rgba(255, 255, 255, 0.62),
    rgba(255, 255, 255, 0.2)
  );
  border: 1px solid var(--glass-border);
  box-shadow:
    0 35px 80px rgba(143, 45, 86, 0.28),
    inset 0 1px 0 rgba(255, 255, 255, 0.6);
  backdrop-filter: blur(22px);
  transform-style: preserve-3d;
  transition: transform 0.18s ease, box-shadow 0.18s ease;
  overflow: hidden;
}

.card-3d::before {
  content: "";
  position: absolute;
  inset: -2px;
  background: linear-gradient(
    120deg,
    transparent,
    rgba(255, 255, 255, 0.45),
    transparent
  );
  transform: translateX(-100%);
  animation: shine 6s ease-in-out infinite;
  pointer-events: none;
}

@keyframes shine {
  0%, 55% {
    transform: translateX(-100%);
  }
  75%, 100% {
    transform: translateX(100%);
  }
}

.ribbon {
  display: inline-block;
  padding: 12px 24px;
  border-radius: 999px;
  background: linear-gradient(135deg, var(--wine), var(--pink));
  color: var(--white);
  font-weight: 600;
  letter-spacing: 0.5px;
  box-shadow: 0 12px 28px rgba(143, 45, 86, 0.28);
  transform: translateZ(55px);
}

.hero {
  margin-top: 32px;
  text-align: center;
  transform: translateZ(70px);
}

.subtitle {
  text-transform: uppercase;
  letter-spacing: 3px;
  color: var(--wine);
  font-size: 0.82rem;
  font-weight: 700;
  margin-bottom: 16px;
}

h1 {
  font-family: "Great Vibes", cursive;
  font-size: clamp(3.4rem, 8vw, 7rem);
  line-height: 0.95;
  color: var(--wine);
  text-shadow: 0 12px 32px rgba(143, 45, 86, 0.22);
  margin-bottom: 22px;
}

.hero-text {
  max-width: 760px;
  margin: 0 auto;
  font-size: 1.1rem;
  line-height: 1.9;
}

.glass-section,
.design-tips {
  margin-top: 38px;
  padding: 32px;
  border-radius: 28px;
  background: var(--glass);
  border: 1px solid var(--glass-border);
  box-shadow: 0 18px 45px rgba(143, 45, 86, 0.12);
  transform: translateZ(45px);
}

h2 {
  font-size: 1.7rem;
  color: var(--wine);
  margin-bottom: 18px;
}

.letter p,
.promise p,
.design-tips li {
  font-size: 1rem;
  line-height: 1.9;
  margin-bottom: 16px;
}

.reasons {
  margin-top: 42px;
  transform: translateZ(60px);
}

.reason-grid {
  display: grid;
  grid-template-columns: repeat(3, 1fr);
  gap: 22px;
  margin-top: 22px;
}

.reason-card {
  position: relative;
  min-height: 230px;
  padding: 30px;
  border-radius: 28px;
  background: linear-gradient(
    145deg,
    rgba(255, 255, 255, 0.74),
    rgba(255, 214, 224, 0.42)
  );
  border: 1px solid rgba(255, 255, 255, 0.6);
  box-shadow:
    0 22px 50px rgba(143, 45, 86, 0.16),
    inset 0 1px 0 rgba(255, 255, 255, 0.7);
  transition: transform 0.35s ease, box-shadow 0.35s ease;
  transform-style: preserve-3d;
}

.reason-card:hover {
  transform: translateY(-12px) rotateX(4deg) rotateY(-4deg);
  box-shadow: 0 35px 70px rgba(143, 45, 86, 0.24);
}

.reason-card span {
  display: inline-flex;
  width: 52px;
  height: 52px;
  justify-content: center;
  align-items: center;
  border-radius: 50%;
  background: linear-gradient(135deg, var(--pink), var(--wine));
  color: var(--white);
  font-weight: 700;
  margin-bottom: 20px;
}

.reason-card h3 {
  color: var(--wine);
  margin-bottom: 14px;
  font-size: 1.25rem;
}

.reason-card p {
  line-height: 1.8;
}

.design-tips ul {
  list-style: none;
}

.design-tips li {
  position: relative;
  padding-left: 28px;
}

.design-tips li::before {
  content: "♥";
  position: absolute;
  left: 0;
  color: var(--wine);
}

.love-button {
  display: block;
  margin: 42px auto 0;
  padding: 16px 34px;
  border: none;
  border-radius: 999px;
  background: linear-gradient(135deg, var(--wine), var(--pink));
  color: var(--white);
  font-size: 1rem;
  font-weight: 700;
  cursor: pointer;
  box-shadow: 0 18px 38px rgba(143, 45, 86, 0.3);
  transition: transform 0.3s ease, box-shadow 0.3s ease;
  transform: translateZ(65px);
}

.love-button:hover {
  transform: translateY(-5px) translateZ(65px);
  box-shadow: 0 26px 52px rgba(143, 45, 86, 0.38);
}

.hidden-message {
  max-height: 0;
  opacity: 0;
  overflow: hidden;
  text-align: center;
  margin-top: 22px;
  font-size: 1.05rem;
  line-height: 1.8;
  color: var(--wine);
  transition: all 0.7s ease;
}

.hidden-message.show {
  max-height: 180px;
  opacity: 1;
}

#particles {
  position: fixed;
  inset: 0;
  z-index: 1;
  pointer-events: none;
}

@media (max-width: 900px) {
  .card-3d {
    padding: 34px 22px;
  }

  .reason-grid {
    grid-template-columns: 1fr;
  }

  .glass-section,
  .design-tips {
    padding: 24px;
  }
}

@media (max-width: 520px) {
  .scene {
    padding: 30px 14px;
  }

  h1 {
    font-size: 3.5rem;
  }

  .hero-text,
  .letter p,
  .promise p,
  .design-tips li {
    font-size: 0.95rem;
  }
}
